{
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev --no-clear",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"devDependencies": {
		"nuxt": "^3.0.0",
		"typescript": "^4.9.4",
		"vue-tsc": "^1.0.24",
		"wiwiblob": "link:../node-bindings"
	}
}
